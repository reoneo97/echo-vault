global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "echovault-backend"
    static_configs:
      # "backend" resolves via Docker's internal DNS when all services
      # are on the same compose network
      - targets: ["backend:8000"]
    metrics_path: "/metrics"
